# Face Recognition Camera Configuration
# This file allows you to configure camera settings without modifying the code

# CAMERA DEVICE CONFIGURATION
# Specify which camera to use. Set to -1 for auto-detection.
# Examples:
#   camera_index = 0    (use /dev/video0)
#   camera_index = 2    (use /dev/video2)
#   camera_index = -1   (auto-detect first available)
camera_index = -1

# CAMERA RANGE FOR AUTO-DETECTION
# When camera_index is -1, the application scans this range of /dev/video* devices
# Useful if you want to limit scanning to specific devices
# Default: 0 to 15 (scans /dev/video0 through /dev/video15)
camera_min_index = 0
camera_max_index = 15

# CAMERA RESOLUTION
# Default resolution to set for the camera
# Common resolutions: 320x240, 640x480, 1280x720, 1920x1080
camera_width = 640
camera_height = 480

# CAMERA FRAME RATE (FPS)
# Target frames per second for camera capture
# Common values: 15, 24, 30, 60
camera_fps = 30

# ENABLE/DISABLE AUTO-LOAD
# Set to 1 to auto-load test image on startup
# Set to 0 to require manual image loading
auto_load = 0

# NOTES:
# - Lines starting with # are comments
# - Empty lines are ignored
# - Command-line arguments override settings in this file
# - Example: ./face_recognition -c 2 will use camera 2, ignoring camera_index setting
